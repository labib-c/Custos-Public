import json
import pandas as pd

def convert_jsons(example, background):
    background_df = pd.DataFrame()
    for example in background.each():
        example.val()
        #pd.read_json

def get_custos_score(example, background):
    convert_jsons(example, background)